

# # Cấu hình file
# config.file.base-path=./songs

# # Cấu hình jobs
# config.jobs.clear-songs.cron=*/5 * * * *  # Every 5 minutes, see https://crontab.guru

# # Cấu hình JWT
# config.jwt.private.key=classpath:app.key
# config.jwt.public.key=classpath:app.pub
# config.jwt.expiration=86400  # in seconds

# # Cấu hình JobRunr
# org.jobrunr.background-job-server.enabled=true
# org.jobrunr.dashboard.enabled=true

# # Cấu hình Spring DataSource (PostgreSQL)
# spring.datasource.url=jdbc:postgresql://localhost:5332/music
# spring.datasource.username=vkaelin
# spring.datasource.password=password
# spring.datasource.driver-class-name=org.postgresql.Driver

# # Cấu hình file upload
# spring.servlet.multipart.max-file-size=10MB
# spring.servlet.multipart.max-request-size=10MB

# # Cấu hình Spring JPA (Hibernate)
# spring.jpa.hibernate.ddl-auto=create-drop
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.properties.hibernate.format_sql=true
# spring.jpa.show-sql=true

# # Cấu hình Springdoc (Swagger UI)
# springdoc.swagger-ui.enabled=true
# springdoc.swagger-ui.path=/swagger-ui.html
# springdoc.api-docs.path=/api-docs



# Cấu hình Spring DataSource (PostgreSQL)
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# Cấu hình Spring JPA (Hibernate)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true

# Cấu hình SMTP
spring.mail.host=${SPRING_MAIL_HOST}
spring.mail.port=${SPRING_MAIL_PORT}